# Crypto Bank Angular Starter

A dark themed crypto-banking style Angular project with:

- Sign in page (`/signin`)
- Sign up page (`/signup`)
- Dashboard (`/dashboard`)

## Run locally

```bash
npm install
npm start
```

Then open `http://localhost:4200`.
